# Relationship schema

Relationship types are defined in `graph/relationships/__init__.py`.

Allowed edges are validated by `GraphValidator` before insertion.

Base migrations: `graphs/migrations/002_relationships.cypher`
